import type { PhaseContent } from "./types";

export const phase3Content: PhaseContent = {
  phaseId: 3,
  color: "#EC4899",
  accent: "#F472B6",
  lessons: [
    {
      phaseId: 3, lessonId: 1,
      title: "Transformer Components",
      subtitle: "LayerNorm、GELU、FeedForward——組裝 Transformer Block 的核心零件",
      type: "concept",
      duration: "60 min",
      objectives: [
        "從零實作 Layer Normalization，理解其穩定訓練的原理",
        "實作 GELU 激活函數，比較其與 ReLU 的差異",
        "建構 FeedForward 網路，理解 4x 擴展比例的設計考量",
        "組裝完整的 Pre-Norm Transformer Block，包含殘差連接",
      ],
      sections: [
        {
          title: "Layer Normalization",
          blocks: [
            { type: "paragraph", text: "上一個 Phase 我們造好了 Attention 模組——token 們終於可以互相交流了。但光會開會不夠，開完會還得各自「消化」和「總結」。今天我們要造三個配件：LayerNorm（會議前的暖身）、GELU（思考時的開關）、FeedForward（獨立消化的過程），然後把它們和 Attention 組裝成一個完整的 Transformer Block。就像搭樂高——每個零件都不複雜，但組合起來就是一個強大的引擎。" },
            { type: "paragraph", text: "在深度神經網路中，每一層的輸入分布會隨訓練不斷變化（internal covariate shift）。Layer Normalization 透過對每個樣本的特徵維度進行正規化，穩定訓練過程、加速收斂。" },
            { type: "paragraph", text: "與 Batch Normalization 不同，LayerNorm 在 feature dimension 上計算統計量，不依賴 batch 中其他樣本。這使它在序列長度可變、batch size 較小的 NLP 任務中表現更好。" },
            { type: "heading", level: 3, text: "LayerNorm 公式" },
            { type: "code", language: "python", code: "# LayerNorm 的數學定義\n# y = gamma * (x - mean) / sqrt(var + eps) + beta\n#\n# mean = x.mean(dim=-1, keepdim=True)\n# var  = x.var(dim=-1, keepdim=True, unbiased=False)\n#\n# gamma: 可學習的縮放參數，初始化為 1\n# beta:  可學習的偏移參數，初始化為 0\n# eps:   防止除零的小常數（通常 1e-5）" },
            { type: "callout", variant: "info", text: "gamma 和 beta 是 per-feature 的可學習參數（shape = (d_model,)），讓模型可以在正規化之後恢復表達能力。如果模型發現某個特徵不需要正規化，它可以學到 gamma=原始std, beta=原始mean 來「撤銷」正規化。" },
            { type: "heading", level: 3, text: "Pre-Norm vs Post-Norm" },
            { type: "paragraph", text: "原始 Transformer（Vaswani et al. 2017）使用 Post-Norm 架構：先經過子層（Attention 或 FFN），再做 LayerNorm。GPT-2 改用 Pre-Norm：先做 LayerNorm，再經過子層。Pre-Norm 使梯度流動更順暢，訓練更穩定。" },
            { type: "table", headers: ["特性", "Post-Norm (原始 Transformer)", "Pre-Norm (GPT-2)"], rows: [
              ["LayerNorm 位置", "子層之後", "子層之前"],
              ["殘差連接", "x + Sublayer(x) → LN", "x + Sublayer(LN(x))"],
              ["訓練穩定性", "較差，需要 warmup", "較好，梯度流更順暢"],
              ["最終 LayerNorm", "不需要", "需要在最後一層之後加 LN"],
              ["代表模型", "BERT, 原始 Transformer", "GPT-2, GPT-3"],
            ]},
            { type: "callout", variant: "tip", text: "Pre-Norm 之所以更穩定，是因為殘差路徑上沒有 normalization——梯度可以直接通過殘差連接流回早期層。這類似於 ResNet 中 identity shortcut 的效果。" },
            { type: "callout", variant: "quote", text: "💡 講師心得：Pre-Norm vs Post-Norm 不只是學術爭論。GPT-2 之後幾乎所有主流模型（GPT-3、LLaMA、Mistral）都用 Pre-Norm，原因是它讓殘差流（residual stream）保持「乾淨」——資訊可以暢通無阻地流過整個網路。Post-Norm 則像在高速公路上加了收費站，每一層都對資訊做了變換，導致深層模型訓練困難。" },
          ],
        },
        {
          title: "GELU Activation",
          blocks: [
            { type: "paragraph", text: "GELU（Gaussian Error Linear Unit）是 GPT 系列模型使用的激活函數。與 ReLU 直接截斷負值不同，GELU 對輸入施加一個隨輸入值平滑變化的「門控」。直覺上，GELU 根據輸入的值來決定「保留多少」——越大的正值保留越多，越小的負值衰減越多。" },
            { type: "heading", level: 3, text: "GELU 近似公式" },
            { type: "code", language: "python", code: "# GPT-2 使用的近似形式：\n# GELU(x) = 0.5 * x * (1 + tanh(sqrt(2/pi) * (x + 0.044715 * x^3)))\n#\n# 其中 sqrt(2/pi) ≈ 0.7978845608\n#\n# 這個近似公式避免了計算高斯 CDF（erf 函數），\n# 在 GPU 上更高效。" },
            { type: "heading", level: 3, text: "GELU vs ReLU 比較" },
            { type: "paragraph", text: "ReLU 在 x=0 處有不可微的硬拐點，且所有負值都被完全丟棄（dead neuron 問題）。GELU 則是處處平滑的，負值不會被完全丟棄，梯度信號更好。" },
            { type: "diagram", content: "GELU vs ReLU:\n\n  輸出 ▲\n   3  │            ╱ ReLU\n      │           ╱\n   2  │          ╱    ·· GELU\n      │         ╱  ··\n   1  │        ╱··\n      │      ·╱\n   0  │···──╱─────────── 輸入\n      │  · ╱\n  -0.2│·· ╱\n      └──────────────►\n     -3  -1   0   1   3\n\nReLU: max(0, x)         — 硬截斷，x<0 時梯度為 0\nGELU: 0.5x(1+tanh(...)) — 平滑門控，負值小幅保留" },
            { type: "callout", variant: "info", text: "GELU 可以看作是一種「軟性 ReLU」：它根據輸入值的「統計顯著性」（與標準高斯分佈比較）來決定是否保留該值。輸入越大，越有可能被保留。" },
          ],
        },
        {
          title: "FeedForward Network",
          blocks: [
            { type: "paragraph", text: "Transformer 中的 FeedForward Network（FFN）是一個簡單的兩層 MLP，應用在每個 token 的表示上（position-wise）。它的作用是在 attention 捕捉了 token 間的關係之後，對每個 token 的表示進行非線性變換。" },
            { type: "heading", level: 3, text: "4x 擴展比例" },
            { type: "paragraph", text: "FFN 的標準設計是：第一層將維度從 d_model 擴展到 4 * d_model，經過 GELU 激活，第二層再壓縮回 d_model。這個 4 倍擴展比例是 Transformer 原始論文中的設計選擇，後來被大多數模型沿用。" },
            { type: "code", language: "python", code: "# FFN 結構：\n# Linear(d_model → 4*d_model) → GELU → Linear(4*d_model → d_model) → Dropout\n#\n# 例如 GPT-2 small: d_model=768\n#   Linear(768 → 3072) → GELU → Linear(3072 → 768)\n#   參數量: 768*3072 + 3072 + 3072*768 + 768 = ~4.7M per block" },
            { type: "callout", variant: "tip", text: "為什麼要擴展再壓縮？這讓 FFN 可以在更高維度的空間中進行非線性變換。類似人腦的「發散-收斂」思維模式：先在更大的空間中探索可能性，再壓縮到原始維度。" },
            { type: "callout", variant: "tip", text: "💡 講師心得：如果說 Attention 是 token 之間的「溝通」，FFN 就是每個 token 的「思考」。最新研究（Geva et al. 2021）發現 FFN 層實際上充當了一個巨大的鍵值記憶體——第一層矩陣是 key（匹配模式），第二層是 value（存儲知識）。這就是為什麼模型知道「巴黎是法國的首都」——這個知識存在 FFN 的參數裡。" },
          ],
        },
        {
          title: "TransformerBlock 組裝",
          blocks: [
            { type: "paragraph", text: "一個 Pre-Norm Transformer Block 由兩個子層組成：Multi-Head Attention 和 FeedForward Network。每個子層都有自己的 LayerNorm 和殘差連接。" },
            { type: "heading", level: 3, text: "Pre-Norm Transformer Block 資料流" },
            { type: "diagram", content: "Pre-Norm Transformer Block:\n\n  輸入 x ─────────────────────────────┐\n    │                                  │ (殘差連接 1)\n    ▼                                  │\n  LayerNorm (ln1)                      │\n    │                                  │\n    ▼                                  │\n  Multi-Head Attention (attn)          │\n    │                                  │\n    ▼                                  │\n  Dropout                             │\n    │                                  │\n    ▼                                  │\n  ╔═══════════════╗                    │\n  ║  x = x + out  ║◄──────────────────┘\n  ╚═══════════════╝\n    │ ──────────────────────────────┐\n    │                               │ (殘差連接 2)\n    ▼                               │\n  LayerNorm (ln2)                   │\n    │                               │\n    ▼                               │\n  FeedForward (ffn)                 │\n    │                               │\n    ▼                               │\n  Dropout                          │\n    │                               │\n    ▼                               │\n  ╔═══════════════╗                 │\n  ║  x = x + out  ║◄───────────────┘\n  ╚═══════════════╝\n    │\n    ▼\n  輸出 x" },
            { type: "code", language: "python", code: "# Pre-Norm Transformer Block 的 forward 邏輯：\n#\n# def forward(self, x):\n#     # 子層 1: Multi-Head Attention\n#     x = x + self.dropout(self.attn(self.ln1(x)))\n#     # 子層 2: FeedForward\n#     x = x + self.dropout(self.ffn(self.ln2(x)))\n#     return x" },
            { type: "callout", variant: "warning", text: "殘差連接的加法對象是原始的 x，不是經過 LayerNorm 的 x。Pre-Norm 的精髓在於：LN 只影響子層的輸入，不影響殘差路徑。這是穩定訓練的關鍵。" },
            { type: "callout", variant: "quote", text: "💡 講師心得：殘差連接是深度學習最重要的發明之一。把它想像成一條「資訊高速公路」——即使 Attention 或 FFN 子層完全失效（輸出零），資訊仍然可以沿著殘差路徑傳到後面的層。這就是為什麼我們可以堆疊 100+ 層而不會梯度消失。每一層只需要學習「增量修正」，而不是從頭重建所有資訊。" },
            { type: "callout", variant: "quote", text: "🤔 思考題：我們的 Transformer Block 用 Pre-Norm（先 LayerNorm 再 sublayer）。但如果你仔細看最後的 output head 前面，還有一個額外的 LayerNorm——為什麼最後還要加一層？如果去掉它會怎樣？（提示：想想殘差流的數值範圍會怎麼隨層數增長。）" },
          ],
        },
      ],
      exercises: [
        {
          id: "layernorm", title: "TODO 1: LayerNorm",
          description: "從零實作 Layer Normalization。在 __init__ 中建立可學習的 gamma（ones）和 beta（zeros）參數。在 forward 中計算 mean、variance，正規化後套用 affine transform。",
          labFile: "labs/phase3_transformer/phase_3/transformer.py",
          hints: [
            "gamma 使用 nn.Parameter(torch.ones(d_model))",
            "beta 使用 nn.Parameter(torch.zeros(d_model))",
            "mean 和 var 都在 dim=-1 上計算，keepdim=True",
            "使用 unbiased=False 計算 variance",
          ],
          pseudocode: "__init__:\n  self.gamma = nn.Parameter(ones(d_model))\n  self.beta = nn.Parameter(zeros(d_model))\n  self.eps = eps\n\nforward(x):\n  mean = x.mean(dim=-1, keepdim=True)\n  var = x.var(dim=-1, keepdim=True, unbiased=False)\n  x_norm = (x - mean) / sqrt(var + eps)\n  return gamma * x_norm + beta",
        },
        {
          id: "gelu", title: "TODO 2: GELU Activation",
          description: "實作 GELU 的近似公式：0.5 * x * (1 + tanh(sqrt(2/pi) * (x + 0.044715 * x^3)))。",
          labFile: "labs/phase3_transformer/phase_3/transformer.py",
          hints: [
            "math.sqrt(2.0 / math.pi) ≈ 0.7978845608",
            "一行就能寫完：0.5 * x * (1 + torch.tanh(...))",
            "GELU(0) = 0，可用此驗證",
          ],
          pseudocode: "forward(x):\n  return 0.5 * x * (1 + tanh(sqrt(2/pi) * (x + 0.044715 * x^3)))",
        },
        {
          id: "feedforward", title: "TODO 3: FeedForward Network",
          description: "建構兩層 MLP：Linear(d_model → d_ff) → GELU → Linear(d_ff → d_model) → Dropout。d_ff 預設為 4 * d_model。",
          labFile: "labs/phase3_transformer/phase_3/transformer.py",
          hints: [
            "如果 d_ff 為 None，設為 4 * d_model",
            "使用你剛實作的 GELU 類別",
            "可以用 nn.Sequential 串接所有層",
          ],
          pseudocode: "__init__:\n  d_ff = d_ff or 4 * d_model\n  self.net = Sequential(\n    Linear(d_model, d_ff),\n    GELU(),\n    Linear(d_ff, d_model),\n    Dropout(dropout)\n  )\n\nforward(x):\n  return self.net(x)",
        },
        {
          id: "transformer_block", title: "TODO 4: TransformerBlock",
          description: "組裝 Pre-Norm Transformer Block：兩個子層各有 LayerNorm + 殘差連接。forward 中的順序為 x = x + dropout(attn(ln1(x)))，然後 x = x + dropout(ffn(ln2(x)))。",
          labFile: "labs/phase3_transformer/phase_3/transformer.py",
          hints: [
            "需要兩個 LayerNorm 實例（ln1, ln2）",
            "MultiHeadAttention 已在 Phase 2 中實作，直接引入使用",
            "殘差連接：加法的左邊是原始 x",
            "TransformerBlock(d_model=32, n_heads=4, block_size=10) 用於測試",
          ],
          pseudocode: "__init__:\n  self.ln1 = LayerNorm(d_model)\n  self.attn = MultiHeadAttention(d_model, n_heads, block_size, dropout)\n  self.ln2 = LayerNorm(d_model)\n  self.ffn = FeedForward(d_model, dropout=dropout)\n  self.dropout = Dropout(dropout)\n\nforward(x):\n  x = x + self.dropout(self.attn(self.ln1(x)))\n  x = x + self.dropout(self.ffn(self.ln2(x)))\n  return x",
        },
      ],
      acceptanceCriteria: [
        "LayerNorm 輸出的 mean ≈ 0、variance ≈ 1（在 feature dimension 上）",
        "LayerNorm 有 2 個可學習參數（gamma, beta）",
        "GELU(0) = 0，GELU(x) > 0 for large positive x",
        "FeedForward 輸入輸出 shape 相同 (batch, seq_len, d_model)",
        "FeedForward 支持自訂 d_ff 參數",
        "TransformerBlock 輸出 shape == 輸入 shape",
        "TransformerBlock 的殘差連接確保輸出不為全零",
        "TransformerBlock 支持不同序列長度（1, 5, 10, 20）",
      ],
      references: [
        { title: "Layer Normalization", description: "Ba et al. 2016 — LayerNorm 的原始論文，詳細分析了正規化對訓練的影響", url: "https://arxiv.org/abs/1607.06450" },
        { title: "Gaussian Error Linear Units (GELUs)", description: "Hendrycks & Gimpel 2016 — GELU 的原始論文，分析其與 dropout 的隱含關聯", url: "https://arxiv.org/abs/1606.08415" },
        { title: "On Layer Normalization in the Transformer Architecture", description: "Xiong et al. 2020 — 深入比較 Pre-Norm 和 Post-Norm 對訓練穩定性的影響", url: "https://arxiv.org/abs/2002.04745" },
        { title: "Language Models are Unsupervised Multitask Learners", description: "Radford et al. 2019 — GPT-2 論文，使用 Pre-Norm Transformer 架構", url: "https://cdn.openai.com/better-language-models/language_models_are_unsupervised_multitask_learners.pdf" },
      ],
    },
    {
      phaseId: 3, lessonId: 2,
      title: "GPT Model Assembly",
      subtitle: "將所有元件組裝成完整的 GPT 語言模型",
      type: "concept",
      duration: "45 min",
      objectives: [
        "定義 GPTConfig dataclass，管理所有模型超參數",
        "組裝 token embedding + position embedding + N 層 Transformer Block + lm_head",
        "理解並實作 weight tying 技術",
        "實作完整的 forward pass，包含可選的 loss 計算",
      ],
      sections: [
        {
          title: "GPTConfig 設計",
          blocks: [
            { type: "paragraph", text: "恭喜你走到了這裡！前三課的零件已經全部造好了：Tokenizer 把文字變成數字，Embedding 把數字變成向量，Attention 讓向量互相交流，FFN 讓每個向量獨立思考。現在，就像組裝一台電腦——CPU、記憶體、硬碟都有了，我們要把它們接到主機板上，變成一台能開機的完整機器。這就是 GPT Model Assembly。" },
            { type: "paragraph", text: "一個好的模型配置系統讓你可以透過改變幾個數字就切換不同規模的模型。GPTConfig 使用 Python 的 @dataclass 裝飾器，將所有超參數集中管理，並提供合理的預設值。" },
            { type: "code", language: "python", code: "@dataclass\nclass GPTConfig:\n    vocab_size: int = 50257    # GPT-2 BPE 詞彙表大小\n    block_size: int = 1024     # 最大序列長度（context window）\n    d_model:    int = 768      # embedding 維度\n    n_heads:    int = 12       # attention head 數量\n    n_layers:   int = 12       # Transformer block 層數\n    dropout:  float = 0.1      # dropout 機率" },
            { type: "table", headers: ["模型", "d_model", "n_heads", "n_layers", "參數量"], rows: [
              ["GPT-2 Small", "768", "12", "12", "124M"],
              ["GPT-2 Medium", "1024", "16", "24", "355M"],
              ["GPT-2 Large", "1280", "20", "36", "774M"],
              ["GPT-2 XL", "1600", "25", "48", "1558M"],
            ]},
            { type: "callout", variant: "info", text: "注意 d_model 必須能被 n_heads 整除（d_k = d_model / n_heads）。GPT-2 Small 中每個 head 處理 768/12 = 64 維的子空間。" },
          ],
        },
        {
          title: "GPT 完整架構",
          blocks: [
            { type: "paragraph", text: "GPT 模型的資料流非常清晰：Token IDs 經過 embedding 層轉換為向量，加上位置編碼，然後通過 N 個 Transformer Block 逐步提煉表示，最終通過線性投影頭輸出每個位置的下一個 token 的機率分佈。" },
            { type: "diagram", content: "GPT 完整架構：\n\n  Token IDs: [15496, 2159, 318, 257, 1365]\n      │\n      ▼\n  ╔══════════════════════════════════════╗\n  ║  Token Embedding  (vocab_size × d)  ║\n  ╚══════════════════════════════════════╝\n      │\n      ▼  +  Position Embedding (block_size × d)\n  ╔══════════════════════════════════════╗\n  ║         Dropout                      ║\n  ╚══════════════════════════════════════╝\n      │\n      ▼\n  ┌──────────────────────────────────────┐\n  │  Transformer Block 1                 │\n  │  ┌─ LN → Multi-Head Attention ─ +   │\n  │  └─ LN → FeedForward ──────── +     │\n  ├──────────────────────────────────────┤\n  │  Transformer Block 2                 │\n  │  ┌─ LN → Multi-Head Attention ─ +   │\n  │  └─ LN → FeedForward ──────── +     │\n  ├──────────────────────────────────────┤\n  │           ...                        │\n  ├──────────────────────────────────────┤\n  │  Transformer Block N                 │\n  │  ┌─ LN → Multi-Head Attention ─ +   │\n  │  └─ LN → FeedForward ──────── +     │\n  └──────────────────────────────────────┘\n      │\n      ▼\n  ╔══════════════════════════════════════╗\n  ║  Final LayerNorm (ln_f)              ║\n  ╚══════════════════════════════════════╝\n      │\n      ▼\n  ╔══════════════════════════════════════╗\n  ║  lm_head: Linear(d_model → vocab)   ║\n  ║  輸出 logits: (B, T, vocab_size)     ║\n  ╚══════════════════════════════════════╝" },
            { type: "callout", variant: "tip", text: "Final LayerNorm 是 Pre-Norm 架構的必要組成。因為 Pre-Norm 在子層之前做正規化，最後一個 Transformer Block 的輸出沒有經過 LN，所以需要在 lm_head 之前加一個 Final LN。" },
            { type: "callout", variant: "quote", text: "💡 講師心得：GPT 模型的結構驚人地簡單——只是把 Phase 1-3 的組件堆疊起來。但這個「簡單」正是它的力量所在。工程上的一個重要原則：能用簡單結構解決的問題，不要用複雜結構。GPT 的成功證明了，真正重要的不是花哨的架構，而是足夠大的規模和足夠多的資料。" },
          ],
        },
        {
          title: "Weight Tying",
          blocks: [
            { type: "paragraph", text: "Weight tying（權重共享）是 GPT 中一個重要的技巧：token embedding 矩陣和 lm_head 的線性投影矩陣共用同一組權重。" },
            { type: "paragraph", text: "直覺上，token embedding 將 token ID 映射為 d_model 維的向量（「這個詞的意思是什麼？」），而 lm_head 將 d_model 維的向量映射回 vocab_size 維的 logits（「這個表示最接近哪個詞？」）。這兩個操作在語義上是對稱的，共用權重是合理的。" },
            { type: "code", language: "python", code: "# Token Embedding: (vocab_size, d_model)\n# lm_head:          Linear(d_model, vocab_size, bias=False)\n#\n# Weight tying:\n# self.lm_head.weight = self.token_embedding.weight\n#\n# 效果：\n# - 減少 vocab_size * d_model 個參數\n#   (GPT-2: 50257 * 768 ≈ 38.6M 參數節省)\n# - 通常能提升模型效能\n# - embedding 和 output projection 的語義一致性" },
            { type: "callout", variant: "warning", text: "weight tying 要求 lm_head 的 bias=False，因為 nn.Embedding 沒有 bias。此外 lm_head.weight 的 shape 是 (vocab_size, d_model)，恰好與 embedding.weight 相同。" },
            { type: "callout", variant: "tip", text: "💡 講師心得：Weight tying（共享 token embedding 和 output head 的權重）是一個精妙的設計。直覺上：embedding 層學到的是「token → 語義向量」的映射，而 output head 做的是「語義向量 → token 概率」的反向映射。這兩個映射本質上是同一件事的正反面，所以共享權重不僅省參數，還能提升效果。" },
          ],
        },
        {
          title: "Forward Pass 與 Loss 計算",
          blocks: [
            { type: "paragraph", text: "GPT 的 forward pass 接受 token ID 序列 idx (shape: B, T)，依序通過 embedding、Transformer blocks、最終 LN 和 lm_head，輸出 logits (shape: B, T, vocab_size)。如果提供了 targets，還會計算 cross-entropy loss。" },
            { type: "code", language: "python", code: "def forward(self, idx, targets=None):\n    B, T = idx.shape\n    assert T <= self.config.block_size\n\n    # Embeddings\n    tok_emb = self.token_embedding(idx)           # (B, T, d_model)\n    pos_emb = self.pos_embedding(torch.arange(T)) # (T, d_model)\n    x = self.dropout(tok_emb + pos_emb)           # broadcasting: (B, T, d_model)\n\n    # Transformer Blocks\n    for block in self.blocks:\n        x = block(x)\n\n    # Output\n    x = self.ln_f(x)\n    logits = self.lm_head(x)  # (B, T, vocab_size)\n\n    # Loss (optional)\n    loss = None\n    if targets is not None:\n        loss = F.cross_entropy(\n            logits.view(-1, logits.size(-1)),  # (B*T, vocab_size)\n            targets.view(-1)                    # (B*T,)\n        )\n    return logits, loss" },
            { type: "heading", level: 3, text: "count_parameters" },
            { type: "paragraph", text: "一個實用的工具函數：計算模型中所有可訓練參數的數量。這幫助你驗證模型規模是否符合預期。" },
            { type: "code", language: "python", code: "def count_parameters(self) -> int:\n    return sum(p.numel() for p in self.parameters() if p.requires_grad)" },
            { type: "callout", variant: "quote", text: "🤔 思考題：GPT-2 Small 有 124M 參數。如果我們把層數從 12 增加到 24（其他不變），參數量大概翻倍到 ~250M。但如果改為把 d_model 從 768 增加到 1536（其他不變），參數量會變成多少？哪種擴展方式更有效率？（提示：算算 attention 和 FFN 的參數量分別怎麼隨 d_model 增長。）" },
          ],
        },
      ],
      exercises: [
        {
          id: "gpt_config", title: "TODO 1: GPTConfig",
          description: "定義 GPTConfig dataclass，包含 vocab_size、block_size、d_model、n_heads、n_layers、dropout 六個欄位及其預設值。",
          labFile: "labs/phase3_transformer/phase_3/gpt_model.py",
          hints: [
            "使用 @dataclass 裝飾器",
            "GPT-2 預設值：vocab_size=50257, block_size=1024, d_model=768, n_heads=12, n_layers=12, dropout=0.1",
            "測試會用 GPTConfig() 不帶參數建構，檢查預設值",
          ],
          pseudocode: "@dataclass\nclass GPTConfig:\n    vocab_size: int = 50257\n    block_size: int = 1024\n    d_model: int = 768\n    n_heads: int = 12\n    n_layers: int = 12\n    dropout: float = 0.1",
        },
        {
          id: "gpt_init", title: "TODO 2: GPT.__init__",
          description: "初始化 GPT 模型的所有組件：token embedding、position embedding、dropout、N 個 TransformerBlock（用 nn.ModuleList）、final LayerNorm、lm_head，並實作 weight tying。",
          labFile: "labs/phase3_transformer/phase_3/gpt_model.py",
          hints: [
            "nn.Embedding(vocab_size, d_model) 用於 token 和 position embedding",
            "nn.ModuleList 讓 PyTorch 能正確追蹤所有子模組的參數",
            "weight tying: self.lm_head.weight = self.token_embedding.weight",
            "lm_head 的 bias=False",
          ],
          pseudocode: "self.config = config\nself.token_embedding = Embedding(vocab_size, d_model)\nself.pos_embedding = Embedding(block_size, d_model)\nself.dropout = Dropout(dropout)\nself.blocks = ModuleList([TransformerBlock(...) for _ in range(n_layers)])\nself.ln_f = LayerNorm(d_model)\nself.lm_head = Linear(d_model, vocab_size, bias=False)\nself.lm_head.weight = self.token_embedding.weight  # weight tying",
        },
        {
          id: "gpt_forward", title: "TODO 3: GPT.forward",
          description: "實作 forward pass：token + position embeddings → dropout → TransformerBlock × N → Final LN → lm_head → logits。如果提供 targets，計算 cross-entropy loss。",
          labFile: "labs/phase3_transformer/phase_3/gpt_model.py",
          hints: [
            "assert T <= self.config.block_size 防止超過最大序列長度",
            "position indices: torch.arange(T, device=idx.device)",
            "logits.view(-1, logits.size(-1)) 和 targets.view(-1) 用於 cross_entropy",
            "沒有 targets 時 loss = None",
          ],
          pseudocode: "B, T = idx.shape\nassert T <= block_size\ntok_emb = self.token_embedding(idx)\npos_emb = self.pos_embedding(arange(T, device=idx.device))\nx = self.dropout(tok_emb + pos_emb)\nfor block in self.blocks:\n    x = block(x)\nx = self.ln_f(x)\nlogits = self.lm_head(x)\nloss = None\nif targets is not None:\n    loss = cross_entropy(logits.view(-1, vocab_size), targets.view(-1))\nreturn logits, loss",
        },
        {
          id: "count_parameters", title: "TODO 4: count_parameters",
          description: "計算模型中所有可訓練參數的總數。",
          labFile: "labs/phase3_transformer/phase_3/gpt_model.py",
          hints: [
            "使用 self.parameters() 遍歷所有參數",
            "p.numel() 返回 tensor 中元素的數量",
            "只計算 requires_grad=True 的參數",
          ],
          pseudocode: "return sum(p.numel() for p in self.parameters() if p.requires_grad)",
        },
      ],
      acceptanceCriteria: [
        "GPTConfig() 不帶參數時，預設值為 vocab_size=50257, d_model=768, n_heads=12, n_layers=12",
        "GPT forward 輸出 logits shape 為 (B, T, vocab_size)",
        "不提供 targets 時 loss 為 None",
        "提供 targets 時 loss 為正的 scalar",
        "支持序列長度 1 到 block_size",
        "count_parameters 返回正整數",
        "梯度可以正常流動（loss.backward() 成功）",
      ],
      references: [
        { title: "Language Models are Unsupervised Multitask Learners", description: "Radford et al. 2019 — GPT-2 論文，完整架構描述", url: "https://cdn.openai.com/better-language-models/language_models_are_unsupervised_multitask_learners.pdf" },
        { title: "Using the Output Embedding to Improve Language Models", description: "Press & Wolf 2017 — Weight tying 的理論基礎與實驗驗證", url: "https://arxiv.org/abs/1608.05859" },
        { title: "nanoGPT", description: "Andrej Karpathy 的極簡 GPT 實作，從零開始只用約 300 行 PyTorch", url: "https://github.com/karpathy/nanoGPT" },
      ],
    },
  ],
};

export const phase4Content: PhaseContent = {
  phaseId: 4,
  color: "#F59E0B",
  accent: "#FBBF24",
  lessons: [
    {
      phaseId: 4, lessonId: 1,
      title: "Training Loop",
      subtitle: "從 Loss 函數到完整訓練迴圈——讓 GPT 真正開始學習",
      type: "concept",
      duration: "75 min",
      objectives: [
        "理解 cross-entropy loss 在語言模型中的意義",
        "實作 cosine learning rate schedule with linear warmup",
        "實作 loss 估計函數與 checkpoint 儲存/載入",
        "建構完整的訓練迴圈，整合 AdamW、gradient clipping、evaluation",
      ],
      sections: [
        {
          title: "Cross-Entropy Loss",
          blocks: [
            { type: "paragraph", text: "模型組裝完成了，但現在它什麼都不會——所有參數都是隨機初始化的，輸出完全是胡言亂語。就像一個剛出生的嬰兒，有大腦的硬體結構，但沒有任何知識。Pre-training 就是讓這個「嬰兒」讀遍整個網際網路，從中學會語言的規律。今天我們要搭建完整的訓練管線：計算 loss、更新參數、調整學習率、儲存進度。這是最考驗工程能力的一個 Phase。" },
            { type: "paragraph", text: "語言模型的訓練目標是最大化正確 token 的預測機率。Cross-entropy loss 衡量模型輸出的機率分佈與真實 token 的 one-hot 分佈之間的差異。loss 越低，模型預測越準確。" },
            { type: "paragraph", text: "具體來說，在每個位置 t，模型輸出一個 vocab_size 維的 logits 向量。Cross-entropy loss = -log(softmax(logits)[correct_token])。loss 的理論下限（perfect prediction）是 0，對隨機初始化的模型來說，初始 loss ≈ ln(vocab_size)。" },
            { type: "code", language: "python", code: "# Cross-entropy loss 的計算\n# 模型輸出 logits: (B, T, vocab_size)\n# 目標 targets: (B, T)\n#\n# PyTorch 的 F.cross_entropy 期望：\n#   input:  (N, C) 其中 N = B*T, C = vocab_size\n#   target: (N,)\n#\n# loss = F.cross_entropy(\n#     logits.view(-1, logits.size(-1)),  # (B*T, vocab_size)\n#     targets.view(-1)                    # (B*T,)\n# )\n#\n# 對 GPT-2 (vocab_size=50257):\n# 隨機初始化時 loss ≈ ln(50257) ≈ 10.82" },
            { type: "callout", variant: "info", text: "Perplexity = exp(loss) 是語言模型常用的評估指標。Perplexity 可以直覺地理解為：模型在每個位置平均「猶豫」於多少個選項。Perplexity = 50257 表示完全隨機猜測。" },
            { type: "callout", variant: "quote", text: "💡 講師心得：Cross-entropy loss 背後的直覺是資訊論。它衡量的是：模型的預測分布和真實分布之間的「驚訝程度」。如果模型對正確答案給了高概率，loss 就低（不驚訝）；反之 loss 就高（很驚訝）。訓練的過程就是讓模型越來越「不驚訝」——越來越能預期真實的文本。" },
          ],
        },
        {
          title: "AdamW Optimizer",
          blocks: [
            { type: "paragraph", text: "AdamW 是目前訓練 Transformer 最常用的優化器。它是 Adam 的改良版本，將 weight decay 從梯度更新中解耦（decoupled weight decay），避免 Adam 的自適應學習率干擾正則化效果。" },
            { type: "heading", level: 3, text: "SGD vs Adam vs AdamW" },
            { type: "table", headers: ["特性", "SGD", "Adam", "AdamW"], rows: [
              ["動量", "可選（手動設定）", "一階 + 二階動量", "一階 + 二階動量"],
              ["自適應學習率", "否", "是（per-parameter）", "是（per-parameter）"],
              ["Weight Decay", "等同 L2 正則化", "與自適應 LR 耦合", "與自適應 LR 解耦"],
              ["超參數", "lr, momentum", "lr, β₁, β₂, ε, wd", "lr, β₁, β₂, ε, wd"],
              ["收斂速度", "慢但穩定", "快", "快且正則化更好"],
              ["Transformer 適用性", "差", "好", "最佳"],
            ]},
            { type: "heading", level: 3, text: "為什麼 Decoupled Weight Decay 很重要？" },
            { type: "paragraph", text: "在 Adam 中，weight decay 被加到梯度上，然後被自適應學習率縮放。這意味著對於梯度小的參數，weight decay 的效果會被放大；梯度大的參數，weight decay 效果被減弱。這不是我們想要的——我們希望 weight decay 均勻地作用於所有參數。AdamW 將 weight decay 移到梯度更新之後獨立執行，解決了這個問題。" },
            { type: "code", language: "python", code: "# AdamW 在 PyTorch 中的使用\noptimizer = torch.optim.AdamW(\n    model.parameters(),\n    lr=3e-4,            # 峰值學習率\n    betas=(0.9, 0.999), # 動量衰減係數\n    eps=1e-8,           # 數值穩定性\n    weight_decay=0.01,  # 解耦的權重衰減\n)" },
            { type: "callout", variant: "tip", text: "Transformer 訓練的典型超參數：lr=3e-4（小模型）到 6e-4（大模型），weight_decay=0.01~0.1，betas=(0.9, 0.95) 或 (0.9, 0.999)。" },
            { type: "callout", variant: "tip", text: "💡 講師心得：AdamW 的「W」（decoupled weight decay）是一個常被忽略但很重要的細節。在原始 Adam 中，L2 正則化和 weight decay 是等價的。但 Adam 的自適應學習率打破了這個等價性——L2 正則化的效果會被 Adam 的分母縮放掉。Loshchilov & Hutter 發現，直接在權重上施加 decay（而不是加到梯度裡）效果好得多。這就是 AdamW 的核心改進。" },
          ],
        },
        {
          title: "Cosine Learning Rate Schedule",
          blocks: [
            { type: "paragraph", text: "學習率排程（LR schedule）在 Transformer 訓練中至關重要。最常用的是 cosine schedule with linear warmup：先線性增加學習率（warmup），再按餘弦曲線逐漸降低到最小值。" },
            { type: "heading", level: 3, text: "Warmup 的意義" },
            { type: "paragraph", text: "訓練剛開始時，模型權重是隨機初始化的，梯度方向高度不穩定。如果一開始就用大學習率，容易造成訓練發散。Linear warmup 讓學習率從 0 開始緩慢增加，給模型時間「穩定下來」後再加速學習。" },
            { type: "heading", level: 3, text: "Schedule 公式" },
            { type: "code", language: "python", code: "def get_lr(step, warmup_steps, max_steps, max_lr, min_lr):\n    # 階段 1: Linear Warmup (step < warmup_steps)\n    #   lr = max_lr * (step / warmup_steps)\n    #\n    # 階段 2: Cosine Decay (warmup_steps <= step <= max_steps)\n    #   progress = (step - warmup_steps) / (max_steps - warmup_steps)\n    #   lr = min_lr + 0.5 * (max_lr - min_lr) * (1 + cos(pi * progress))\n    #\n    # 階段 3: 超過 max_steps\n    #   lr = min_lr" },
            { type: "diagram", content: "Cosine LR Schedule with Warmup:\n\n  lr ▲\n     │\nmax ─│        ╭──╮\n     │       ╱    ╲\n     │      ╱      ╲\n     │     ╱        ╲\n     │    ╱          ╲\n     │   ╱            ╲\n     │  ╱              ╲\n     │ ╱                ╲\nmin ─│╱                  ╲──────────\n     │\n     └────┬──────────────┬──────────► step\n          │              │\n       warmup         max_steps\n       steps\n\n     ◄─────►◄────────────►◄────────►\n      Linear    Cosine      Constant\n      Warmup    Decay       (min_lr)" },
            { type: "callout", variant: "info", text: "常見設定：warmup_steps = 總步數的 5~10%，min_lr = max_lr 的 1/10。例如 max_steps=1000 時，warmup_steps=100，max_lr=3e-4，min_lr=3e-5。" },
            { type: "callout", variant: "quote", text: "💡 講師心得：學習率排程是訓練中最容易出錯的超參數。Warmup 的作用是防止模型在 Adam 的二階矩估計還不準確時（前幾百步）做出過大的更新。Cosine decay 則提供了一個平滑的退火過程——前期大步探索，後期小步精修。如果你的 loss 曲線在訓練初期不穩定，第一個該調的就是 warmup steps。" },
          ],
        },
        {
          title: "Gradient Clipping",
          blocks: [
            { type: "paragraph", text: "梯度爆炸（gradient explosion）是深度網路訓練中的常見問題，在 Transformer 中尤為嚴重。Gradient clipping 通過限制梯度的整體 L2 norm 來防止單次更新過大。" },
            { type: "code", language: "python", code: "# Gradient clipping 的使用\n# 在 loss.backward() 之後、optimizer.step() 之前\ntorch.nn.utils.clip_grad_norm_(\n    model.parameters(),\n    max_norm=1.0  # 常用值\n)\n# 如果梯度的 L2 norm > max_norm，\n# 所有梯度會被等比例縮小，使 norm 恰好等於 max_norm" },
            { type: "callout", variant: "warning", text: "clip_grad_norm_ 必須在 backward() 之後、step() 之前調用。順序錯誤會導致 clipping 無效或 runtime error。" },
          ],
        },
        {
          title: "Loss Estimation 與 Checkpointing",
          blocks: [
            { type: "paragraph", text: "訓練中需要定期評估模型表現。estimate_loss 函數在驗證集上計算平均 loss，用於追蹤是否過擬合。評估時需要關閉 dropout 並停止梯度計算以節省記憶體。" },
            { type: "code", language: "python", code: "# Loss 估計的關鍵點\n@torch.no_grad()  # 停止梯度追蹤\ndef estimate_loss(model, dataloader, eval_steps, device='cpu'):\n    model.eval()   # 關閉 dropout、設為評估模式\n    losses = []\n    for i, (x, y) in enumerate(dataloader):\n        if i >= eval_steps:\n            break\n        x, y = x.to(device), y.to(device)\n        _, loss = model(x, y)\n        losses.append(loss.item())\n    model.train()  # 恢復訓練模式（重要！）\n    return sum(losses) / len(losses)" },
            { type: "heading", level: 3, text: "Checkpoint 儲存與載入" },
            { type: "paragraph", text: "Checkpoint 保存訓練的完整狀態（模型權重、優化器狀態、當前步數、loss），讓訓練可以在中斷後恢復。" },
            { type: "code", language: "python", code: "# 儲存 checkpoint\ndef save_checkpoint(model, optimizer, step, loss, path):\n    Path(path).parent.mkdir(parents=True, exist_ok=True)\n    torch.save({\n        'model_state_dict': model.state_dict(),\n        'optimizer_state_dict': optimizer.state_dict(),\n        'step': step,\n        'loss': loss,\n    }, path)\n\n# 載入 checkpoint\ndef load_checkpoint(path, model, optimizer=None):\n    ckpt = torch.load(path, map_location='cpu', weights_only=True)\n    model.load_state_dict(ckpt['model_state_dict'])\n    if optimizer is not None:\n        optimizer.load_state_dict(ckpt['optimizer_state_dict'])\n    return ckpt" },
            { type: "callout", variant: "tip", text: "為什麼要保存 optimizer state？因為 Adam 類優化器有每個參數的動量（momentum）和二階動量（variance）。不保存這些，恢復訓練時會從頭計算動量，效果如同重新 warmup。" },
          ],
        },
        {
          title: "完整訓練迴圈",
          blocks: [
            { type: "paragraph", text: "訓練迴圈將上述所有元件串聯起來。每一步的流程是：取批次 → 前向傳播 → 計算 loss → 反向傳播 → gradient clipping → 更新權重 → 調整學習率。" },
            { type: "diagram", content: "Training Loop Flow:\n\n  ╔═══════════════════════════╗\n  ║  初始化                    ║\n  ║  - Model → device          ║\n  ║  - AdamW optimizer         ║\n  ║  - DataLoader iterator     ║\n  ╚═══════════════════════════╝\n              │\n              ▼\n  ┌───────────────────────────┐◄──────────────────┐\n  │  Step = 0, 1, ..., N-1    │                   │\n  ├───────────────────────────┤                   │\n  │                           │                   │\n  │  1. lr = get_lr(step)     │                   │\n  │     → 更新 optimizer lr   │                   │\n  │                           │                   │\n  │  2. batch = next(iter)    │                   │\n  │     → x, y to device      │                   │\n  │                           │                   │\n  │  3. logits, loss          │                   │\n  │     = model(x, y)         │                   │\n  │                           │                   │\n  │  4. loss.backward()       │                   │\n  │                           │                   │\n  │  5. clip_grad_norm_       │                   │\n  │     (max_norm=1.0)        │                   │\n  │                           │                   │\n  │  6. optimizer.step()      │                   │\n  │     optimizer.zero_grad() │                   │\n  │                           │                   │\n  │  7. if step % eval == 0:  │                   │\n  │     → estimate val loss   │                   │\n  │     → print progress      │                   │\n  │                           │                   │\n  │  8. if step % ckpt == 0:  │                   │\n  │     → save_checkpoint     │                   │\n  │                           │                   │\n  └───────────────────────────┘───── step++ ──────┘\n              │\n              ▼\n  ╔═══════════════════════════╗\n  ║  Return final losses      ║\n  ╚═══════════════════════════╝" },
            { type: "heading", level: 3, text: "DataLoader 循環" },
            { type: "paragraph", text: "訓練步數通常大於 DataLoader 中的批次數量。當 DataLoader 的 iterator 耗盡時，需要重新建立 iterator 來循環使用資料。最簡單的方式是使用 itertools.cycle()。" },
            { type: "code", language: "python", code: "import itertools\n\n# 方法 1: itertools.cycle（推薦）\ntrain_iter = itertools.cycle(train_loader)\nfor step in range(max_steps):\n    x, y = next(train_iter)\n    ...\n\n# 方法 2: 手動重建 iterator\ntrain_iter = iter(train_loader)\nfor step in range(max_steps):\n    try:\n        x, y = next(train_iter)\n    except StopIteration:\n        train_iter = iter(train_loader)\n        x, y = next(train_iter)" },
            { type: "heading", level: 3, text: "TrainConfig" },
            { type: "paragraph", text: "與 GPTConfig 類似，TrainConfig 將所有訓練超參數集中到一個 dataclass 中。這使得實驗管理和超參數搜索更加方便。" },
            { type: "code", language: "python", code: "@dataclass\nclass TrainConfig:\n    max_steps: int = 1000         # 總訓練步數\n    batch_size: int = 4           # 批次大小\n    learning_rate: float = 3e-4   # 峰值學習率\n    min_lr: float = 3e-5          # 最低學習率\n    warmup_steps: int = 100       # warmup 步數\n    eval_interval: int = 100      # 每 N 步評估一次\n    eval_steps: int = 10          # 每次評估的批次數\n    checkpoint_dir: str = 'checkpoints'\n    checkpoint_interval: int = 500\n    max_grad_norm: float = 1.0    # 梯度裁剪上限\n    device: str = 'cpu'" },
            { type: "callout", variant: "warning", text: "訓練迴圈中最常見的 bug：忘記 zero_grad()（梯度累積）、eval 後忘記切回 train mode、LR schedule 的 step 計數錯誤。建議用 assert 檢查關鍵的中間狀態。" },
            { type: "callout", variant: "quote", text: "🤔 思考題：我們的 loss 函數是 cross-entropy，它衡量的是模型對「下一個 token」的預測能力。但你有沒有想過——在訓練初期，序列開頭的 token 的 loss 會比結尾的高還是低？為什麼？（提示：想想 causal mask 的效果——位置 0 只能看到自己，位置 T-1 能看到前面所有 token。）" },
          ],
        },
      ],
      exercises: [
        {
          id: "get_lr", title: "TODO 1: get_lr (cosine schedule)",
          description: "實作 cosine learning rate schedule with linear warmup。三個階段：linear warmup → cosine decay → constant min_lr。",
          labFile: "labs/phase4_pretraining/phase_4/utils.py",
          hints: [
            "step >= max_steps 時直接返回 min_lr",
            "warmup 階段：lr = max_lr * (step / warmup_steps)",
            "cosine 階段的 progress = (step - warmup_steps) / (max_steps - warmup_steps)",
            "cosine 公式：min_lr + 0.5 * (max_lr - min_lr) * (1 + cos(pi * progress))",
            "warmup_steps=0 時跳過 warmup，直接進入 cosine decay",
          ],
          pseudocode: "if step >= max_steps:\n    return min_lr\nif step < warmup_steps:\n    return max_lr * step / warmup_steps\nprogress = (step - warmup_steps) / (max_steps - warmup_steps)\nreturn min_lr + 0.5 * (max_lr - min_lr) * (1 + cos(pi * progress))",
        },
        {
          id: "estimate_loss", title: "TODO 2: estimate_loss",
          description: "在 eval mode 下計算平均 loss。設定 model.eval()，遍歷至多 eval_steps 個 batch，計算平均 loss，最後恢復 model.train()。",
          labFile: "labs/phase4_pretraining/phase_4/utils.py",
          hints: [
            "@torch.no_grad() 裝飾器已經加上了",
            "記得在結束時恢復 model.train()",
            "用 enumerate + break 或 itertools.islice 限制 batch 數量",
            "loss.item() 將 tensor 轉為 Python float",
            "處理 dataloader 批次數 < eval_steps 的情況",
          ],
          pseudocode: "model.eval()\nlosses = []\nfor i, (x, y) in enumerate(dataloader):\n    if i >= eval_steps: break\n    x, y = x.to(device), y.to(device)\n    _, loss = model(x, y)\n    losses.append(loss.item())\nmodel.train()\nreturn sum(losses) / len(losses)",
        },
        {
          id: "checkpoints", title: "TODO 3: save_checkpoint / load_checkpoint",
          description: "實作 checkpoint 的儲存與載入。save 需建立目錄並用 torch.save 儲存 dict；load 需用 torch.load 載入並恢復 model 和 optimizer 狀態。",
          labFile: "labs/phase4_pretraining/phase_4/utils.py",
          hints: [
            "save: Path(path).parent.mkdir(parents=True, exist_ok=True)",
            "save: dict 包含 model_state_dict, optimizer_state_dict, step, loss",
            "load: 使用 torch.load(path, map_location='cpu', weights_only=True)",
            "load: model.load_state_dict(ckpt['model_state_dict'])",
            "load: optimizer 是可選的，if optimizer is not None 才載入",
          ],
          pseudocode: "# save_checkpoint:\nPath(path).parent.mkdir(parents=True, exist_ok=True)\ntorch.save({\n    'model_state_dict': model.state_dict(),\n    'optimizer_state_dict': optimizer.state_dict(),\n    'step': step, 'loss': loss\n}, path)\n\n# load_checkpoint:\nckpt = torch.load(path, map_location='cpu', weights_only=True)\nmodel.load_state_dict(ckpt['model_state_dict'])\nif optimizer: optimizer.load_state_dict(ckpt['optimizer_state_dict'])\nreturn ckpt",
        },
        {
          id: "train_loop", title: "TODO 4: TrainConfig + train()",
          description: "定義 TrainConfig dataclass 和完整的 train() 函數。train() 建立 AdamW optimizer，用 itertools.cycle 循環 DataLoader，每步執行 forward → backward → clip → step → LR update，並定期 eval 和保存 checkpoint。",
          labFile: "labs/phase4_pretraining/phase_4/train.py",
          hints: [
            "TrainConfig 需要 11 個欄位，都有預設值（見 docstring）",
            "用 itertools.cycle(train_loader) 處理 DataLoader 循環",
            "LR 更新：optimizer.param_groups[0]['lr'] = get_lr(...)",
            "順序：get_lr → forward → backward → clip_grad_norm_ → step → zero_grad",
            "val_loader 可能為 None，eval 時需檢查",
            "返回 dict 包含 'final_train_loss' 和 'final_val_loss'",
          ],
          pseudocode: "# TrainConfig: @dataclass with 11 fields\n\n# train():\nmodel.to(config.device)\noptimizer = AdamW(model.parameters(), lr=config.learning_rate)\ntrain_iter = cycle(train_loader)\n\nfor step in range(config.max_steps):\n    lr = get_lr(step, warmup_steps, max_steps, max_lr, min_lr)\n    optimizer.param_groups[0]['lr'] = lr\n\n    x, y = next(train_iter)\n    x, y = x.to(device), y.to(device)\n    logits, loss = model(x, y)\n    loss.backward()\n    clip_grad_norm_(model.parameters(), config.max_grad_norm)\n    optimizer.step()\n    optimizer.zero_grad(set_to_none=True)\n\n    if step % eval_interval == 0:\n        val_loss = estimate_loss(...) if val_loader else None\n        print(f'step {step}: train_loss={loss:.4f} val_loss={val_loss:.4f}')\n\n    if step % checkpoint_interval == 0:\n        save_checkpoint(model, optimizer, step, loss.item(), path)\n\nreturn {'final_train_loss': loss.item(), 'final_val_loss': val_loss}",
        },
      ],
      acceptanceCriteria: [
        "get_lr 在 step=0 時返回 0（或接近 0），warmup 結束時返回 max_lr",
        "get_lr 在 cosine 中點返回 (max_lr + min_lr) / 2",
        "get_lr 超過 max_steps 後返回 min_lr",
        "get_lr 始終返回非負值",
        "estimate_loss 返回 Python float，且為正值",
        "estimate_loss 結束後模型恢復 train mode",
        "save_checkpoint 建立檔案，load_checkpoint 恢復完整狀態（包括 step 和 loss）",
        "checkpoint roundtrip 後模型權重完全一致",
        "TrainConfig 有所有必要欄位且預設值合理",
        "train() 完成後模型權重確實改變",
        "train() 在 val_loader=None 時也能正常運行",
        "gradient clipping 不會讓訓練崩潰（即使用極大 learning rate）",
      ],
      references: [
        { title: "Decoupled Weight Decay Regularization", description: "Loshchilov & Hutter 2019 — AdamW 論文，解釋為什麼 decoupled weight decay 優於 L2 正則化", url: "https://arxiv.org/abs/1711.05101" },
        { title: "Attention Is All You Need", description: "Vaswani et al. 2017 — 原始 Transformer 論文，提出 warmup + inverse sqrt decay 的 LR schedule", url: "https://arxiv.org/abs/1706.03762" },
        { title: "Chinchilla: Training Compute-Optimal Large Language Models", description: "Hoffmann et al. 2022 — 探討最佳的 training tokens 數量與模型大小的比例", url: "https://arxiv.org/abs/2203.15556" },
        { title: "nanoGPT", description: "Andrej Karpathy 的極簡 GPT 訓練實作，包含完整的 training loop", url: "https://github.com/karpathy/nanoGPT" },
      ],
    },
  ],
};
