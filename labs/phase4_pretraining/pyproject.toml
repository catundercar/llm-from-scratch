[project]
name = "phase4-pretraining"
version = "0.1.0"
description = "Phase 4: Pretraining a GPT model from scratch"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0",
    "pytest>=7.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
