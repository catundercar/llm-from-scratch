[project]
name = "phase7-lora"
version = "0.1.0"
description = "Phase 7: LoRA (Low-Rank Adaptation) for efficient fine-tuning"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0",
    "pytest>=7.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
